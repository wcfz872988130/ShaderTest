using UnityEngine;
using System.Collections;

public class JsonFile : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	public static string ParseJson(string curpath)
	{
		//string path = "Assets" + "/" + curpath;
		//TextAsset file=UnityEditor.AssetDatabase.LoadAssetAtPath (path,typeof(TextAsset)) as TextAsset;
		//return file.text;
		return null;
	}
}
